# Install the .desktop file
install_data(
  'musek.desktop',
  install_dir: join_paths(get_option('datadir'), 'applications')
)

# Install the icon (assuming musek.png is 256x256)
install_data(
  'musek.png',
  install_dir: join_paths(get_option('datadir'), 'icons', 'hicolor', '256x256', 'apps')
)
